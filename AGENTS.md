# 编码规则

1. **禁止打补丁思维**：发现问题时，先定位根因，再从源头修复；严禁在现有逻辑外层堆叠 if/else、try/catch 或临时变量来绕过问题。
2. **改动必有理由**：每次代码变更必须能用一句话说清"为什么改"，无法说清则重新分析。
3. **文档与代码同步**：任何影响接口、行为、配置或依赖的变更，必须在同一次提交中同步更新相关文档。
4. **宁可不写，不可乱写**：不确定的逻辑不要用注释掩盖，要明确标记 `TODO(reason):` 并说明原因。
5. **先理解，后动手**：在修改代码前，必须先阅读并理解被修改模块的完整上下文。
6. **最小化变更**：只修改达成目标所必须的代码，不"顺手"优化其他地方。
7. **不造轮子**：项目中已有的工具函数/组件，优先复用，不重新实现。
8. **不假设**：不确定某个函数的行为时，先查看其实现和测试，不猜测。
9. **变更透明**：每次修改后，输出一个简短的影响分析：「修改了什么 → 影响了哪些模块 → 需要同步更新哪些文档」。

## 代码规范

协作原则：
- 永远先 plan，后 coding；
- 重大修改前先解释原因、改动文件、可能影响；
- 一次只处理一个明确目标；
- 不要无意义新增依赖；
- 保持代码简洁、可复用、易读；
- 默认支持移动端与暗黑模式；
- 文案避免 AI 套话，要具体、自然。
